# No Laughing Matter

I saw a bunch of `A` and `H` in the file and tought what if it is binary?
And I replaced `A` with `0` and `H` with `1`.
After that I used the from binary function on cyberchef and got the flag.

flag: `bronco{UFUNNYLMAOLOLXDIJBOLROFLHAHA}`